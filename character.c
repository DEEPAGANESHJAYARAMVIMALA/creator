#include <stdio.h>
void main()
{
    char str[100];
    int count=0;
    int i;
    printf("enter the string");
    gets(str);
    for(i=0;str[i]!==null;i++)
    {
        if(a[i]==" ")
        {
            count++;
           
        }
         printf("count the number of characters:%d",count);
    }
}
